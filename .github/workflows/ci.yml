name: Build CI

on:
  push:
  pull_request:
  release:
    types: [published]
  check_suite:
    types: [rerequested]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*
      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh
      - name: Execute CMake
        run: |
          mkdir build
          cd build
          cmake ..
      - name: Execute Make
        run: |
          cd build
          make -j
      - name: Save build folders
        uses: actions/upload-artifact@v2
        with:
          name: buildfiles
          path: |
            build

  test-biquad:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test biquad
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/biquad
  test-symbols_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test symbols_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/symbols_0
  test-lathe:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test lathe
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/lathe
  test-and-or-not-mux_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test and-or-not-mux_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/and-or-not-mux_0
  test-ilowpass:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test ilowpass
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/ilowpass
  test-rtapi_printf_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test rtapi_printf_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/rtapi_printf_0
  test-stepgen_1:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test stepgen_1
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/stepgen_1
  test-motion:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test motion
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/motion
  test-linuxcncrsh:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test linuxcncrsh
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/linuxcncrsh
  test-loadrt_1:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test loadrt_1
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/loadrt_1
  test-halmodule_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test halmodule_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/halmodule_0
  test-ccomp:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test ccomp
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/ccomp
  test-mdi-queue-length:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test mdi-queue-length
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/mdi-queue-length
  test-symbols_1:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test symbols_1
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/symbols_1
  test-abort:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test abort
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/abort
  test-statbuffer-g5x-abort:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test statbuffer-g5x-abort
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/statbuffer-g5x-abort
  test-remap:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test remap
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/remap
  test-pyhal:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test pyhal
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/pyhal
  test-tclsh-extensions:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test tclsh-extensions
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/tclsh-extensions
  test-tool-info:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test tool-info
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/tool-info
  test-twopass:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test twopass
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/twopass
  test-near_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test near_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/near_0
  test-abs_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test abs_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/abs_0
  test-twopass-personality:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test twopass-personality
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/twopass-personality
  test-oword:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test oword
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/oword
  test-multiclick:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test multiclick
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/multiclick
  test-limit3:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test limit3
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/limit3
  test-alias_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test alias_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/alias_0
  test-flipflop_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test flipflop_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/flipflop_0
  test-tlo:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test tlo
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/tlo
  test-t0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test t0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/t0
  test-startup-state:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test startup-state
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/startup-state
  test-halcompile:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test halcompile
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/halcompile
  test-linuxcncrsh-tcp:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test linuxcncrsh-tcp
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/linuxcncrsh-tcp
  test-stepgen_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test stepgen_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/stepgen_0
  test-single-step:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test single-step
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/single-step
  test-counter-encoder_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test counter-encoder_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/counter-encoder_0
  test-source_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test source_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/source_0
  test-module-loading:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test module-loading
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/module-loading
  test-save_1:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test save_1
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/save_1
  test-mdi-queue:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test mdi-queue
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/mdi-queue
  test-m70-m73:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test m70-m73
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/m70-m73
  test-hard-limits:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test hard-limits
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/hard-limits
  test-realtime-math:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test realtime-math
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/realtime-math
  test-halrun-lock:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test halrun-lock
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/halrun-lock
  test-stepgen_2:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test stepgen_2
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/stepgen_2
  test-io-startup:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test io-startup
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/io-startup
  test-build:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test build
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/build
  test-modparam_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test modparam_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/modparam_0
  test-hal-link-unlink:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test hal-link-unlink
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/hal-link-unlink
  test-hm2-idrom:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test hm2-idrom
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/hm2-idrom
  test-edge:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test edge
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/edge
  test-mux:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test mux
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/mux
  test-halui:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test halui
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/halui
  test-loadrt_2:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test loadrt_2
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/loadrt_2
  test-timedelay_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test timedelay_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/timedelay_0
  test-save_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test save_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/save_0
  test-toolchanger:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test toolchanger
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/toolchanger
  test-basic:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test basic
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/basic
  test-motion-logger:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test motion-logger
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/motion-logger
  test-trajectory-planner:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test trajectory-planner
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/trajectory-planner
  test-overrun:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test overrun
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/overrun
  test-rs274ngc-startup:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test rs274ngc-startup
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/rs274ngc-startup
  test-uspace:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test uspace
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/uspace
  test-halrun-getopt-reset:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test halrun-getopt-reset
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/halrun-getopt-reset
  test-halmodule_1:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test halmodule_1
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/halmodule_1
  test-mdi-while-queuebuster-waitflag:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test mdi-while-queuebuster-waitflag
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/mdi-while-queuebuster-waitflag
  test-lowlevel:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test lowlevel
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/lowlevel
  test-threads_0:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test threads_0
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/threads_0
  test-threads_1:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test threads_1
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/threads_1
  test-interp:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - run: git fetch --recurse-submodules=no https://github.com/linuxcnc/linuxcnc refs/tags/*:refs/tags/*

      - name: Install build dependecies
        run: ./scripts/travis-install-build-deps.sh

      - name: Download build
        uses: actions/download-artifacts@v2
        with:
          - name: buildfiles

      - name: Run test interp
        run: |
          cd ./build/bin
          ./test-linuxcnc.sh -v ../../tests/interp
